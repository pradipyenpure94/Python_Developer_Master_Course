"""
Create a list of numbers and manually iterate through
it using iter() and next().
"""

numbers = [10, 20, 30, 40, 50]

iterators = iter(numbers)

print(next(iterators))
print(next(iterators))
print(next(iterators))
